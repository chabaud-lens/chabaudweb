Place official brand icons (SVG preferred) in this folder with these exact filenames:

- instagram.svg  — official Instagram glyph or logo (SVG)
- tiktok.svg     — official TikTok glyph or logo (SVG)
- whatsapp.svg   — official WhatsApp glyph or logo (SVG)
- mail.svg       — envelope or brand mark for email (optional)

Notes:
- SVGs keep the site sharp and small; prefer monochrome (single-color) SVGs so `currentColor` styling works.
- If you don't have SVGs, PNGs are acceptable, but make sure sizes and backgrounds match the site's minimal aesthetic.
- Recommended sources: official brand resources pages (Instagram Brand Resources, TikTok Brand Resources, WhatsApp Brand Resources).

Example: `public/icons/instagram.svg` will be referenced in the contact block as `/icons/instagram.svg`.

If you want, I can fetch and add the official SVGs for you (requires permission to download assets).